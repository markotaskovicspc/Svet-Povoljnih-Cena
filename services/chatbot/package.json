{
  "name": "spc-social-chatbot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env.local src/main.mjs",
    "test": "node --test test/*.test.mjs",
    "smoke": "node --env-file-if-exists=.env.local scripts/smoke.mjs"
  },
  "dependencies": {
    "@openai/agents": "0.18.0",
    "pg": "^8.20.0",
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
