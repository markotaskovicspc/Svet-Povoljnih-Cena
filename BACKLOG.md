# Backlog

Format: `~/.claude/os/BACKLOG-SPEC.md`. Sources: QA-REPORT.md, QA-FINDINGS.md,
project memory (badi fiscal state, ERP wiring state). Bug numbers reference
QA-REPORT.md's bug table.

## Phase 1 — launch blockers

- [x] NEEDS-USER: obtain valid MyGLS test credentials. *(2026-07-10: RESOLVED by MyGLS answer — no test env exists for COD; our creds are production creds, valid since Client ID issuance. Testing happens directly in production: created labels are inert until a pickup is announced (najava ≥24h ahead). `MYGLS_ENV=production` set in .env.local; controlled production pass replaces the old test pass — see the [build] item below.)*
- [x] [build] MyGLS controlled production pass. *(2026-07-10: PASSED via direct API round-trip — production creds valid, PrintLabels with COD 1000 RSD succeeded (ParcelNumber 9002486576, PDF returned), status behavior for fresh labels confirmed benign, DeleteLabels verified clean. Caught + fixed real bug: production requires package dimensions, `payload.ts` now sends Height/Width/Length defaults. See QA-FINDINGS 2026-07-10 afternoon.)*
- [ ] [build] MyGLS in-app shipment smoke test: repeat the create→delete loop through the app path (admin action → `createMyGlsShipmentForOrder` → label in `shipment-labels` bucket → delete) now that the payload dimension fix is in — the direct-API pass bypassed the app/DB layer. Accept: shipment row + label object created and cleaned up; recorded in QA-FINDINGS.md.
- [ ] NEEDS-USER: MyGLS pickup contact name + phone for `MYGLS_PICKUP_CONTACT_*` in .env.local. *(2026-07-16: warehouse address supplied as Severna transverzala bb, 15000 Šabac and configured; Marko said the courier contact will follow. Name/phone intentionally left empty so a pickup cannot be booked with placeholder details. Still ask MyGLS how najava is performed: portal/email/API.)*
- [ ] [build] IPS callback route to Payten PGW spec: callback is an unsigned JSON POST — treat as untrusted wake-up ping, gate on pending IPS payment, verify via server→gateway `checkStatus`, always answer 200; remove invented HMAC machinery; rate-limit per-IP/per-order; token-expiry clamp + 401 retry. Accept: `npm run build` green; forged callback with random orderId → 200 with no gateway call and no DB change; real path funnels through `checkPaymentStatus`.
- [ ] NEEDS-USER: reply on the Payten/Raiffeisen thread with merchant logo (`public/logo.jpeg`) + testing IP 178.221.225.75 for whitelist (draft prepared 2026-07-10); wait for whitelist confirmation.
- [ ] [build] IPS test-PGW end-to-end pass (after whitelist): pre-flight `generateToken` curl (record `tokenExpiryTime` units), order → QR redirect → pay in test env → PAID/POTVRDJENO + fiscal + email side-effects, negative paths (cancel/fail/forged/replayed callback), full + partial refund. Callback leg via public tunnel in `IPS_PUBLIC_BASE_URL`. Accept: all steps recorded in QA-FINDINGS.md; watch for non-"00" in-progress responseCodes transiently flipping PENDING→FAILED (add allowlist if the test PGW uses them).
- [x] [build] IPS site-compliance sweep per "Uputstvo za rad IPS internet prodajnog mesta". *(2026-07-10: all 8 points verified/fixed — legal name corrected to registered form, fake MB 20000000 → real 22112597 in `src/lib/merchant.ts`, added šifra delatnosti 4791, "kada se zadužuje račun" section on uslovi-isporuke, IPS-exclusive refund statement on reklamacije; PDV note, logo placements, order specification already OK.)*
- [x] NEEDS-USER: real PIB. *(2026-07-16: Luka supplied `115085587`; updated `src/lib/merchant.ts` and `FISCAL_TIN`.)*
- [x] NEEDS-USER: real bank account + bank name. *(2026-07-16: Luka supplied `265-3310310005375-34`; configured as Raiffeisen banka and removed the remaining hardcoded checkout placeholder.)*
- [ ] NEEDS-USER: get a real RESEND_API_KEY (or empty the placeholder) — order emails currently fail silently against the real API (bug #4).
- [ ] NEEDS-USER: X Express still silent on the najava-specifikacija + webhook-registration email (contract U000328) — if no reply by 2026-07-14, send a follow-up nudge. `/api/order/add` stays stubbed until their spec lands.
- [x] NEEDS-USER: uncheck "Aktivan" for "Kartica (RaiAccept)" at /admin/placanje (bug #6). *(2026-07-10: disabled directly in DB with note; re-enable when RaiAccept env keys land.)*
- [x] [build] Harden env config checks: treat any env value starting with `GET_FROM_` as unconfigured in every provider fallback (Resend, IPS, courier), so placeholder secrets degrade to dev-mode instead of firing real failing API calls. Accept: with `.env.local` placeholders in place, order flow logs email to console instead of calling Resend, and `npm run build` passes. *(2026-07-06: shared `envValue()` in `src/lib/env.ts`, wired into email/fiscal/IPS/bulky/viber configs; verified at runtime — dispatch logged `[email:dev]` instead of calling Resend; build+lint clean.)*
- [x] [build] Convert the `proizvodi` and `kategorije` admin bare forms to `AdminActionForm`/`useActionState` (bug #8 remainder). *(2026-07-10: all six product actions + category upsert/remove converted via `withAdminState`; inline Serbian errors/success; build green; commit cbd4d5b.)*

## Phase 2 — customer-facing gaps

- [x] [build] Reclamation submission UI under "Moj nalog → Reklamacije" (bug #15). *(2026-07-10: page + form with presigned multi-photo upload; live-verified against the real API (400/403 paths); release review confirmed ownership checks/rate limits; commit cbd4d5b. Nice-to-have noted: assert photo-URL order segment matches the reclamation's order.)*
- [x] [build] Admin responsive layout below 768px (bug #13). *(2026-07-10: hamburger + Sheet drawer, desktop unchanged; DataTable already had overflow-x-auto; commit cbd4d5b.)*
- [x] [build] MyGLS in-app shipment smoke test. *(2026-07-10: PASSED with cleanup (parcel 9002486633 created+deleted); caught REAL bug — production returns `PrintLabelsInfoList`, code read `PrintDataInfoList`, so tracking numbers never persisted; fixed in commit cbd4d5b.)*
- [x] [build] Vercel cron wiring. *(2026-07-10: vercel.json schedules + all 10 cron routes accept global CRON_SECRET via shared timing-safe helper; NOTE: sub-daily schedules need Vercel Pro — otherwise move fiscal-retry/payment-expiry to cron-job.org.)*
- [x] [review] Pre-launch release review. *(2026-07-10: verdict SHIP, zero blockers, over the full branch — payments idempotency, cron auth blast radius, admin action auth, badi idempotency/secret handling, reclamation surface, MyGLS persistence. Nice-to-haves: real PIB before first invoice; reclamation photo/order consistency check; confirm scheduler coverage for x-express/webhook-events route.)*

## Phase 3 — fiscalization go-live (badi.rs)

- [x] NEEDS-USER: badi.rs account created + WORKING production creds in .env.local. *(2026-07-10: first key was never saved in the dashboard — second key authenticates fine; GET/POST/DELETE /products verified live. FISCAL_PROVIDER=badi. Note: sandbox is a separate environment (sandbox.badi.rs) needing its own registration — skipped; testing uses invoiceType "training" in production instead.)*
- [ ] NEEDS-USER: badi fiscal processor — receipt issuance returns 400/40080001 "No client with the given storeId or clientId is connected" until a PFR exists. Preferred path (no always-on machine): **V-PFR certificate mode** — accountant/client submits PGJO on ePorezi (prodajno mesto "internet prodaja" for svetpovoljnihcena.rs), requests the bezbednosni element **u elektronskom obliku** (.pfx + PAC) for V-PFR, then set BADI_VPFR_PFX / BADI_VPFR_PASSWORD / BADI_VPFR_PAC in .env.local (code support shipped 2026-07-10; badi api-docs "VPFR certificate mode" headers). Confirm exact pfx header encoding with badi support (info@dvsoftware.rs, greška 40080001). Alternative: Badi LPFR app on an always-on machine — rejected by Luka.
- [ ] [build] badi E2E training-receipt pass once the PFR is connected: set BADI_INVOICE_TYPE=training, issue a sale via the app path (ensureBadiProducts → /fiscalization/receipts), verify response shape (invoiceNumber, sdcDateTime, verificationUrl, base64pdf) and official PDF landing in the `fiscal-receipts` bucket; then refund path with buyerId. Accept: training receipt round-trip recorded in QA-FINDINGS.md; BADI_INVOICE_TYPE reset to normal afterwards.
- [x] NEEDS-USER: create the `fiscal-receipts` Supabase bucket. *(2026-07-06: created — public, PDF-only, matching `order-receipts`; upload/public-fetch/delete round-trip verified.)*
- [x] NEEDS-USER: point an external cron scheduler at `/api/cron/fiscal-retry`. *(2026-07-10: handled via vercel.json crons + CRON_SECRET auto-header; CAVEAT: 15-min schedules need Vercel Pro — on Hobby, put fiscal-retry & payment-expiry on cron-job.org with `Authorization: Bearer CRON_SECRET` instead.)*
- [x] NEEDS-USER: ask the accountant whether IPS prepayment legally requires an "advance" receipt + close-at-delivery instead of `normal` at payment time. *(2026-07-10: ANSWERED — "najkonformnije je da se odmah izda konačan račun, a kod povraćaja refundacioni račun." Final receipt at payment + refund receipt on returns = exactly current behavior (`invoiceType: "normal"` on IPS confirmation, refunds via /admin/fiskalizacija). No advance-receipt flow needed; no code change.)*
- [ ] [build] badi sandbox spike (after creds land): verify `receiptDelivery` response shape, the product-must-preexist assumption, and duplicate-product error text against the real sandbox; fix `src/lib/fiscal/badi.ts` where reality differs. Accept: a sandbox receipt issues successfully and the official PDF lands in the `fiscal-receipts` bucket.
- [ ] [review] Pre-launch release review: fiscal issuance/refund paths, payment methods, checkout order-state machine, courier webhook auth. Accept: verdict SHIP, or BLOCK with a minimum fix list.

## Phase 4 — ERP follow-ups

- [ ] [build] PO PDF generation + send-to-supplier email attachment (send currently only flips status). Accept: sending a PO emails a PDF (console-logged in dev mode) and the PO event trail records it; `npm run build` passes.
- [ ] [build] Transport-cost allocation into BM%/COGS (currently customs-only approximation), per spec §5.1 alongside `src/lib/admin/po.ts`. Accept: receiving a PO with transport costs produces weighted-average COGS that includes freight; `npm run build` passes.
- [ ] [build] Warehouse/lager admin UI over WarehouseStock + StockMovement. Accept: stock levels and movements for a received PO are visible in admin; `npm run build` passes.
- [ ] [build] Dedicated inbound-invoice editor with freight allocation. Accept: an inbound invoice can be edited and posted with freight split across lines; `npm run build` passes.
